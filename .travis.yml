language: swift

osx_image: xcode12.2
env:
  matrix:
    - TEST_SDK=iphonesimulator12.1 OS=12.3 NAME='iPhone XR'


script:
  - set -o pipefail && xcodebuild test -enableCodeCoverage YES -project MatheusLib.xcodeproj -scheme MatheusLib -sdk $TEST_SDK -destination "platform=iOS Simulator,OS=$OS,name=$NAME" ONLY_ACTIVE_ARCH=YES
